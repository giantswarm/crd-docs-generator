# DO NOT EDIT. Generated with:
#
#    devctl
#
#    https://github.com/giantswarm/devctl/blob/1acd23e6a78c21ca61ccbe8a7e5a8a3139feeab5/pkg/gen/input/workflows/internal/file/fix_vulnerabilities.yaml.template
#

name: Fix Go vulnerabilities

on:
  schedule:
    - cron: '0 9 * * 1-5'
  workflow_dispatch:
    inputs:
      branch:
        description: Branch on which to fix vulnerabilities
        required: true
        type: string
      log_level:
        description: Log Level (info / error / debug)
        default: "info"
        type: string

permissions: {}

jobs:
  fix:
    uses: giantswarm/github-workflows/.github/workflows/fix-vulnerabilities.yaml@main
    with:
      branch: ${{ inputs.branch || github.ref }}
      log_level: ${{ inputs.log_level }}
    secrets:
      HERALD_APP_ID: ${{ secrets.HERALD_APP_ID }}
      HERALD_APP_KEY: ${{ secrets.HERALD_APP_KEY }}
      NANCY_USER: ${{ secrets.NANCY_USER }}
      NANCY_TOKEN: ${{ secrets.NANCY_TOKEN }}
